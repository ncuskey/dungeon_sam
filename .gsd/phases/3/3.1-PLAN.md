---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: New Monsters (Rubble & Watcher)

## Objective
Introduce the Rubble and Watcher enemies to the game state and rendering pipelines.

## Context
- .gsd/SPEC.md
- src/types/game.ts
- src/components/EnemyRenderer.tsx
- src/store/gameStore.ts

## Tasks

<task type="auto">
  <name>Expand enemy type definitions</name>
  <files>src/types/game.ts, src/components/EnemyRenderer.tsx</files>
  <action>
    - In `types/game.ts`, expand `Enemy['type']` union to include `'watcher'` and `'rubble'`.
    - In `EnemyRenderer.tsx`, update `getEnemyTexture`: Map `'rubble'` to `'/rubble.png'`, `'watcher'` to `'/The_Watcher.png'`, `'goblin'` to `'/goblin.png'`, and default to `'/imp.png'`.
  </action>
  <verify>npm run check (to verify TS compilation)</verify>
  <done>Types accept the 4 enemy variants and renderer supports their sprites cleanly without incorrect fallbacks.</done>
</task>

<task type="auto">
  <name>Integrate new monsters into spawn logic</name>
  <files>src/store/gameStore.ts</files>
  <action>
    - In `spawnEnemy`, choose randomly from `['imp', 'goblin', 'rubble', 'watcher']`. 
    - Assign stats based on type (e.g., watcher HP: 200, rubble HP: 150, goblin: 60, imp: 100). Sets reasonable `moveCooldown`s.
  </action>
  <verify>Launch game and run around until a Watcher or Rubble enemy spawns</verify>
  <done>Watchers and Rubble appear dynamically inside runtime combat loops.</done>
</task>

## Success Criteria
- [ ] Codebase compiles cleanly with expanded enemy types.
- [ ] Enemies appear ingame featuring their respective 3D sprite textures.
