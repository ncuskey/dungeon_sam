---
phase: 4
plan: fix-texture
wave: 1
gap_closure: true
---

# Fix: Consolidate Texture Mappings

## Problem
There is duplicated logic mapping item/weapon names to their image paths across `ItemRenderer.tsx` and `WeaponOverlay.tsx`.

## Root Cause
No central source of truth exists for game asset paths and texture definitions.

## Tasks

<task type="auto">
  <name>Create Asset Constants</name>
  <files>src/utils/constants.ts, src/components/ItemRenderer.tsx, src/components/WeaponOverlay.tsx</files>
  <action>
    - Create `src/utils/constants.ts` if it doesn't already exist.
    - Export `WEAPON_TEXTURES` and `SHIELD_TEXTURES` mapping objects (e.g., `{ 'Iron Shield': '/shield.png' }`).
    - Refactor `ItemRenderer.tsx` to use the constants instead of inline `if/return` logic.
    - Refactor `WeaponOverlay.tsx` to use the constants instead of inline `if/return` logic.
  </action>
  <verify>grep -r "WEAPON_TEXTURES" src/components/</verify>
  <done>Texture mapping logic is centralized and duplication is removed from components.</done>
</task>
