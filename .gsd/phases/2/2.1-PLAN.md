---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Weapon Visuals (Left/Right Arm)

## Objective
Split the first-person view into distinct left and right arms, reflecting equipped shields and weapons accurately on the screen.

## Context
- .gsd/SPEC.md
- src/components/WeaponOverlay.tsx
- .gsd/phases/2/RESEARCH.md

## Tasks

<task type="auto">
  <name>Refactor overlay layout</name>
  <files>src/components/WeaponOverlay.tsx</files>
  <action>
    - Import `equippedShieldId` from the `useGameStore`.
    - Change the render function to always output a `flex` container holding the left arm and right arm elements, instead of the ternary switch that hides the left arm entirely when a sword is equipped.
    - Set the left arm's `src` to `/shield.png` if a shield is equipped, otherwise `/fist_left.png`.
    - Set the right arm's `src` to the correct weapon image (e.g. `/sword_truth.png`) if a weapon is equipped, otherwise `/fist_right.png`.
    - Apply existing attack animations targeting both images appropriately.
  </action>
  <verify>npm run dev</verify>
  <done>Player sees a shield in their left hand and a sword in their right hand simultaneously.</done>
</task>

## Success Criteria
- [ ] Left arm displays shield or bare fist dynamically.
- [ ] Right arm displays sword or bare fist dynamically.
