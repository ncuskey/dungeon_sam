---
phase: 2
plan: 1
wave: 1
---

# Plan 2.1: Monster Spawn Logic

## Objective
Update the level generator to spawn all monster types, ensuring `watcher` and `imp` (if not already prevalent) appear correctly throughout the dungeon.

## Context
- .gsd/SPEC.md
- src/utils/dungeonGenerator.ts

## Tasks

<task type="auto">
  <name>Update Initial Enemy Spawns</name>
  <files>src/utils/dungeonGenerator.ts</files>
  <action>
    - Update the initial enemy generation logic in `dungeonGenerator.ts` to include `watcher` alongside `imp`, `goblin`, and `rubble`.
    - Adjust the spawn probabilities: e.g. 40% `imp`, 30% `goblin`, 15% `rubble`, 15% `watcher`.
    - Ensure HP and moveCooldown are correctly set for `watcher` and `imp` during generation, matching the template in `spawnEnemy`.
  </action>
  <verify>grep "watcher" src/utils/dungeonGenerator.ts</verify>
  <done>All monster types can spawn during map generation.</done>
</task>

## Success Criteria
- [ ] The dungeon generator spawns `watcher` enemies.
- [ ] `imp` continues to spawn correctly along with new adjustments.
