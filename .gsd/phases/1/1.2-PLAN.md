---
phase: 1
plan: 2
wave: 1
---

# Plan 1.2: Mobile Door Controls & Torch Light Emit Point

## Objective
Enable opening doors on mobile via the touch interact button and adjust torch lighting to emanate from the fire portion of the graphic rather than the base.

## Context
- .gsd/SPEC.md
- src/components/TouchControls.tsx
- src/components/LevelRenderer.tsx

## Tasks

<task type="auto">
  <name>Add mobile door toggle</name>
  <files>src/components/TouchControls.tsx</files>
  <action>
    - Update the INTERACT button `onPointerDown` callback in `TouchControls.tsx`.
    - Check the `gameStore` for `playerPosition`, `playerDirection`, and `map` to inspect the cell exactly one step in front of the player (mimicking `PlayerController.tsx` logic).
    - If the cell is a door (`2` or `3`), invoke `toggleDoor()`. Otherwise, invoke `pickupItem()`.
  </action>
  <verify>npm run test or manual browser check on mobile dimensions</verify>
  <done>Pressing the Interact (✋) button on mobile properly toggles a door in front of the player.</done>
</task>

<task type="auto">
  <name>Offset torch light point</name>
  <files>src/components/LevelRenderer.tsx</files>
  <action>
    - In the `<Torch />` component, locate the `<pointLight />`.
    - Apply a Y position offset (e.g. `position={[0, 0.3, 0]}`) so the light originates near the top of the sprite (flame) instead of the ground/group center.
    - Avoid changing the billboard's position.
  </action>
  <verify>npm run dev</verify>
  <done>The glowing point of light visibly sits at the top of the torch sprites.</done>
</task>

## Success Criteria
- [ ] Players can open doors via the interact button on mobile UI.
- [ ] Torch pointLight aligns with the visible flame of the asset.
