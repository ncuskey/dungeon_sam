---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Door Visuals and Centering

## Objective
Fix door rendering so the image maps correctly depending on which side it's viewed from (hinge vs handle). Ensure doors are perfectly centered within the passageway rather than sitting flush with walls.

## Context
- .gsd/SPEC.md
- src/components/LevelRenderer.tsx
- .gsd/phases/1/RESEARCH.md

## Tasks

<task type="auto">
  <name>Center doors in passage</name>
  <files>src/components/LevelRenderer.tsx</files>
  <action>
    - Review the pivot and offset logic inside the `doors = useMemo` block.
    - Adjust `pivotPosition` and `meshPosition` logic to ensure the `BoxGeometry` aligns identically through the center axis of the gap instead of clipping or misaligning.
    - Avoid changing the core rotation array, just mathematical position offsets.
  </action>
  <verify>npm run dev (Manual check required to see if doors are in center of gap)</verify>
  <done>All map doors sit exactly in the middle of a passageway opening.</done>
</task>

<task type="auto">
  <name>Differentiate hinge and handle textures</name>
  <files>src/components/LevelRenderer.tsx</files>
  <action>
    - Update the single `<meshStandardMaterial map={doorTexture} />` on the door's mesh.
    - Apply an array of materials to the `BoxGeometry` to allow distinct mapping for front and back faces. 
    - Adjust UVs or use a cloned flipped texture so the door handle and hinges display accurately from either side of approach.
  </action>
  <verify>npm run dev (Manual check of both sides of a door)</verify>
  <done>The door visual shows the handle correctly on the same edge from both sides.</done>
</task>

## Success Criteria
- [ ] Doors are centered in their respective passages.
- [ ] Front and back of door textures align logically (handle matches on both sides).
