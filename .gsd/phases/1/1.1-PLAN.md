---
phase: 1
plan: 1.1
wave: 1
---

# Plan 1.1: Project Foundation

## Objective
Initialize the project structure with Vite, React, and Three.js dependencies, establishing the rendering context.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md

## Tasks

<task type="auto">
  <name>Initialize Vite Project</name>
  <files>package.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx</files>
  <action>
    Initialize a new Vite project with React and TypeScript using `npm create vite@latest . -- --template react-ts`.
    Install `three`, `@types/three`, `@react-three/fiber`, `@react-three/drei`.
    Clean up default Vite boilerplate (remove App.css, index.css generic styles, logo imports).
    Ensure `vite.config.ts` is configured correctly.
  </action>
  <verify>npm run dev -- --port 3000 (check for successful build and server start)</verify>
  <done>Project builds, dependencies installed, clean entry point</done>
</task>

<task type="auto">
  <name>Setup Canvas Context</name>
  <files>src/App.tsx, src/components/GameCanvas.tsx</files>
  <action>
    Create `src/components/GameCanvas.tsx` with a basic R3F `&lt;Canvas&gt;` setup.
    Configure the camera (perspective, fov: 75) and basic lights (ambient + point).
    Render a simple rotating cube to verify the 3D context is working.
    Mount `GameCanvas` in `App.tsx`.
    Add basic CSS reset in `index.css` to make Canvas fullscreen (`width: 100vw; height: 100vh; margin: 0; overflow: hidden;`).
  </action>
  <verify>Browser screenshot showing a rotating cube on a black/colored background</verify>
  <done>3D scene renders successfully in browser</done>
</task>

## Success Criteria
- [ ] Dependencies installed (Three, R3F)
- [ ] Dev server runs without errors
- [ ] Fullscreen 3D canvas is visible with test geometry
