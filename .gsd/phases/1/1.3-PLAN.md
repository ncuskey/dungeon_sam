---
phase: 1
plan: 3
wave: 2
---

# Plan 1.3: Starting Equipment & Auto-Pickup

## Objective
Ensure the player begins with a shield automatically equipped and implements QoL auto-pickup when walking over items.

## Context
- .gsd/SPEC.md
- src/store/gameStore.ts

## Tasks

<task type="auto">
  <name>Grant shield at spawn</name>
  <files>src/store/gameStore.ts</files>
  <action>
    - Remove the 'Iron Shield' from being injected into `initialSpawnedItems` on the map.
    - Modify the initial `inventory` state in `useGameStore` to include the `Iron Shield` inside `items`, with `x: -1, y: -1`.
    - Set `equippedShieldId: shieldId` in the starting inventory state.
    - Ensure `resetGame` mirrors this properly.
  </action>
  <verify>npm run dev - check HUD and store state for equipped shield</verify>
  <done>Player spawns with 'Iron Shield' in inventory and equipped.</done>
</task>

<task type="auto">
  <name>Auto-pickup items on step</name>
  <files>src/store/gameStore.ts</files>
  <action>
    - Update `moveForward` and `moveBackward` inside `useGameStore`.
    - After updating the position (`newX, newY`), check if any mapped item shares those coordinates.
    - If an item exists, trigger the `pickupItem` logic (or an internal equivalent) immediately during the move action.
    - Avoid double-triggering sounds if possible.
  </action>
  <verify>npm run dev - place an item in front of spawn and step on it</verify>
  <done>Items completely disappear from ground and enter inventory simply by walking over them.</done>
</task>

## Success Criteria
- [ ] Player starts game with an actively equipped iron shield.
- [ ] Items on the floor are looted by simply stepping onto their tile.
