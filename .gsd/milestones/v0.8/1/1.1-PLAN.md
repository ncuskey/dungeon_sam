---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Weapon Visuals and Drop Logic

## Objective
Integrate missing or partially implemented weapons (Bow and Arrow, Watcher Sword) and verify existing weapon/shield assets map to the correct files.

## Context
- .gsd/SPEC.md
- src/store/gameStore.ts
- src/components/ItemRenderer.tsx
- src/components/WeaponOverlay.tsx

## Tasks

<task type="auto">
  <name>Update Ground Item Rendering</name>
  <files>src/components/ItemRenderer.tsx</files>
  <action>
    - Add explicit mappings for `Watcher Sword` to return `/Watcher Sword.png`.
    - Add explicit mapping for `Bow` to return `/Bow and Arrow.png`.
    - Ensure `Sword of Truth` returns `/sword_truth.png`.
  </action>
  <verify>grep "Watcher" src/components/ItemRenderer.tsx</verify>
  <done>Ground textures correctly map to all unique weapon names instead of defaulting.</done>
</task>

<task type="auto">
  <name>Add Bow and Arrow to Game</name>
  <files>src/store/gameStore.ts, src/components/WeaponOverlay.tsx</files>
  <action>
    - In `gameStore.ts`, add a small chance for a drop in `playerAttack` for `Bow and Arrow` (e.g. from general drop tables).
    - In `WeaponOverlay.tsx`, add a mapping in `getWeaponTexture` to return `/Bow and Arrow.png` when `wName` includes `Bow`.
  </action>
  <verify>grep "Bow and Arrow" src/components/WeaponOverlay.tsx</verify>
  <done>Bow and Arrow can drop from enemies and correctly renders when equipped.</done>
</task>

## Success Criteria
- [ ] Watcher Sword uses its own texture on the ground instead of the default sword.
- [ ] Bow and Arrow is fully integrated as a weapon (drops, renders on ground, renders on HUD).
