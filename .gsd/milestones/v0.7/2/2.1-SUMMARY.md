# Plan 2.1 Summary

**Tasks Completed:**
1. **Refactor overlay layout:** Updated `WeaponOverlay.tsx` to pull both `equippedWeaponId` and `equippedShieldId`. We stripped the binary toggle logic and configured the UI to statically present a flex layout. The left arm actively defaults to `fist_left.png` but upgrades to `/shield.png` if equipped. The right arm defaults to `fist_right.png` but upgrades to `/sword_truth.png` if equipped. Both arms execute attack animations continuously.

**Verification:**
Fully implemented into the react node hierarchy.
