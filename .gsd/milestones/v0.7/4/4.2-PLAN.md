---
phase: 4
plan: 2
wave: 1
---

# Plan 4.2: Sound Consistency Pass

## Objective
Fill missing audio gaps for world interaction events like opening doors and completing a level.

## Context
- .gsd/SPEC.md
- src/audio/SoundManager.ts
- src/store/gameStore.ts

## Tasks

<task type="auto">
  <name>Implement Door and Level Audio</name>
  <files>src/audio/SoundManager.ts, src/store/gameStore.ts</files>
  <action>
    - Open `SoundManager.ts` and author two new methods: `playDoorOpen()` (a low rumble/sweep) and `playLevelComplete()` (a triumphant ascending arpeggio or chime array).
    - Inside `gameStore.ts`, locate `toggleDoor()`. Replace the empty `Note: If we had a sound...` comment with a direct call to `soundManager.playDoorOpen()`.
    - Inside `gameStore.ts` inside `moveForward/moveBackward` exit coordinate intersection, call `soundManager.playLevelComplete()` right before triggering `nextLevel()`.
  </action>
  <verify>npm run dev (Interact with a door and listen for the audio cue)</verify>
  <done>Opening doors and reaching exits produce explicit acoustic feedback.</done>
</task>

## Success Criteria
- [ ] `SoundManager` fully supports door and level progression sounds.
- [ ] Event listeners functionally trigger the corresponding sounds during gameplay without crashing.
