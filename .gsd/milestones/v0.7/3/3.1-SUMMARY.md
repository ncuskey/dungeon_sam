# Plan 3.1 Summary

**Tasks Completed:**
1. **Expand enemy type definitions:** Updated `Enemy` interface inside `src/types/game.ts` to actively accept `'watcher' | 'rubble'`. Rewrote `getEnemyTexture()` inside `EnemyRenderer.tsx` to handle correct texture fallbacks (`/The_Watcher.png` and `/rubble.png`).
2. **Integrate new monsters into spawn logic:** Modified `spawnEnemy` inside `gameStore.ts` to leverage a `Math.random()` roll checking for > 0.9 (`watcher` with 200 HP and 3 cooldown), > 0.7 (`rubble` with 150 HP and 2 cooldown), and so on down properties.

**Verification:**
Fully integrated properly rendering into combat loop and TS compiler.
